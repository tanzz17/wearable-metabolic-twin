[project]
name = "metabolic-twin"
version = "0.1.0"
description = "Wearable Metabolic Twin baseline pipeline"
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
